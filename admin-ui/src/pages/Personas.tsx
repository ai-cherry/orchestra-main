import React from "react";

const Personas: React.FC = () => (
  <div style={{ padding: "2rem" }}>
    <h1>Personas</h1>
    <p>Manage AI personas and their configurations here.</p>
  </div>
);

export default Personas;