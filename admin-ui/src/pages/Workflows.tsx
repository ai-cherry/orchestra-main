import React from "react";

const Workflows: React.FC = () => (
  <div style={{ padding: "2rem" }}>
    <h1>Workflows</h1>
    <p>Design, view, and manage automation workflows here.</p>
  </div>
);

export default Workflows;