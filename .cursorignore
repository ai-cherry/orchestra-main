# === TIER 1: CRITICAL EXCLUSIONS (Performance Impact) ===
# Python virtual environments and dependencies
venv/
.venv/
node_modules/
.yarn/
.npm/

# Python cache and build artifacts
__pycache__/
*.pyc
*.pyo
build/
dist/
*.egg-info/
*.egg

# Large binary files and media (> 10MB impact)
*.bin
*.pt
*.pth
*.onnx
*.h5
*.model
*.joblib
*.pickle
*.pkl
*.mp4
*.mov
*.avi
*.wav
*.mp3
*.zip
*.tar.gz
*.gz
*.rar
*.7z
*.whl

# === TIER 2: DEVELOPMENT ARTIFACTS ===
# Test reports and caches
htmlcov/
.pytest_cache/
.coverage
nosetests.xml
coverage.xml
*.test.js
*.spec.js
test-results/
playwright-report/

# IDE/editor specific (keep shared configs)
.idea/
*.swp
*.swo
*~

# OS-specific files
.DS_Store
Thumbs.db
.directory

# === TIER 3: CLOUD AND INFRASTRUCTURE ===
# Cloud provider artifacts
.pulumi/
.terraform/
terraform.tfstate*
.aws/
.gcp/

# Docker and container artifacts
.docker/
*.dockerfile
docker-compose.override.yml

# Secrets files (if accidentally committed outside .env or designated secret management)
# Add specific credential file names if you know them and they shouldn't be indexed
# Example: credentials.json, service_account_key.json
# Be cautious not to ignore intentionally used credential files if they MUST be in the repo
# and are handled by other security means (though this is generally not recommended).
*.key
*.secret
secrets/
*.pem

# Node.js modules (if any JS/TS parts exist or for linters/tools)
node_modules/

# IPython notebooks (excluding checkpoints already covered)
# *.ipynb # Uncomment if you want to ignore notebooks themselves, not just checkpoints

# General build/distribution artifacts
*.tar
*.whl
*.exe
*.dll
*.so
*.dylib

# Cloud provider specific files (if not part of IaC that should be indexed)
.pulumi/
Pulumi.*.yaml

# Other common ignores
*~
*.swp
*.swo

# Project-specific additions
/.git/
gcp_snapshots/
.ai-memory/
.dev_mode
google-cloud-cli-latest-linux-x86_64.tar.gz
credential_scan_report.json
security-analysis.json
