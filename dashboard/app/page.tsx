"use client";

import { ConversationalInterface } from '@/components/ConversationalInterface';

/**
 * Home Page Component
 * 
 * Main entry point for the AI Orchestrator dashboard.
 * Renders the conversational interface that replaces the traditional admin panel.
 */
export default function HomePage() {
  return <ConversationalInterface />;
}
