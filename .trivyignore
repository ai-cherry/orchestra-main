# Known false positives and accepted risks

# Example format:
# CVE-2022-12345 # Dependency issue in package X that doesn't affect us because we don't use feature Y

# Ignore development-only vulnerabilities
CVE-2021-23727 # Development dependency for pytest, not used in production

# Acceptable risk vulnerabilities (with justification)
# CVE-YYYY-XXXXX # Issue in package Z, mitigation in place with rate limiting