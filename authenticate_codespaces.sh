#!/bin/bash
# This script has been disabled by cleanup_gcp_config.sh
# The original content is saved as authenticate_codespaces.sh.bak
echo "This script has been disabled to prevent conflicts with the new GCP authentication setup."
echo "If you need the original functionality, rename authenticate_codespaces.sh.bak back to authenticate_codespaces.sh"
exit 0
