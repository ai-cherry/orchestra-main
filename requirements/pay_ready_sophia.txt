# Pay Ready/Sophia MCP Server Dependencies
# Sales Intelligence Platform - Production Requirements

# Core async and HTTP
httpx==0.25.2
asyncio-mqtt==0.13.0
aiofiles==23.2.1

# Redis for caching and session management
redis[hiredis]==5.0.1

# Database connections
psycopg2-binary==2.9.9  # PostgreSQL
weaviate-client==3.25.3  # Vector database

# Pinecone for specialized embeddings (if needed)
pinecone-client==2.2.4

# Data processing and validation
pydantic==2.5.2
pandas==2.1.4
numpy==1.24.4

# API integrations
requests==2.31.0
python-dotenv==1.0.0

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# MCP Server base (from legacy)
# Note: Assuming base server is available locally

# Development and testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.13.2
mypy==1.7.1

# Production server
uvicorn[standard]==0.24.0
fastapi==0.104.1
gunicorn==21.2.0

# Logging and monitoring
structlog==23.2.0
sentry-sdk==1.38.0

# Date/time handling
python-dateutil==2.8.2
pytz==2023.3

# Environment and configuration
click==8.1.7
rich==13.7.0  # Better CLI output

# Optional: AI/ML for advanced analytics
openai==1.3.7  # For GPT-based analysis
langchain==0.0.340  # For AI workflows 