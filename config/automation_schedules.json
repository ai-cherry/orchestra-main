{
  "daily_data_sync": {
    "workflow_id": "data_pipeline_workflow",
    "schedule": "0 2 * * *",
    "enabled": true
  },
  "code_review_on_push": {
    "workflow_id": "code_review_workflow",
    "trigger": "git_push",
    "enabled": true
  },
  "weekly_deployment": {
    "workflow_id": "deployment_workflow",
    "schedule": "0 10 * * 1",
    "enabled": false
  }
}