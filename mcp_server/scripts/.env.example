# MCP Server Environment Variables
# Copy this file to .env and modify as needed

# Configuration
MCP_CONFIG_PATH=./custom-config.json  # Path to custom config file (optional)
LOG_LEVEL=INFO                        # Logging level (DEBUG, INFO, WARNING, ERROR)

# API Keys (uncomment and set as needed)
# GEMINI_API_KEY=your_gemini_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Advanced Settings
# MCP_PORT=8080                       # Port to run the server on (default: 8080)
# MCP_HOST=0.0.0.0                    # Host to bind to (default: 0.0.0.0)
# MCP_WORKERS=4                       # Number of worker processes (default: 4)
# MCP_TIMEOUT=120                     # Request timeout in seconds (default: 120)